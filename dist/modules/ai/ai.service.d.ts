import { ConfigService } from '@nestjs/config';
import { AxiosResponse } from 'axios';
import { SystemRulesService, RuleType, ChatMessage } from './utils/system-rules.service';
import { MessageManagerService, VueCodeManagerService } from './utils/message-manager.service';
import { AiLoggerService } from './utils/logger.service';
import { ContinuationDetectorService } from './utils/continuation-detector.service';
import { StreamChatDto, CompletionChatDto, GetModelsDto, GetModelsResponseDto, CozeWorkflowRunDto, CozeWorkflowStreamRunDto, CozeWorkflowResponseDto } from './dto/chat.dto';
export declare class AiService {
    private readonly configService;
    private readonly systemRulesService;
    private readonly messageManagerService;
    private readonly vueCodeManagerService;
    private readonly logger;
    private readonly continuationDetector;
    private readonly siliconflowApiKey;
    private readonly siliconflowBaseUrl;
    private readonly cozeApiKey;
    private readonly cozeBaseUrl;
    constructor(configService: ConfigService, systemRulesService: SystemRulesService, messageManagerService: MessageManagerService, vueCodeManagerService: VueCodeManagerService, logger: AiLoggerService, continuationDetector: ContinuationDetectorService);
    streamChat(streamChatDto: StreamChatDto): Promise<AxiosResponse>;
    private createSmartStreamResponse;
    private handleSmartStreamResponse;
    private performContinuation;
    private extractContentFromChunk;
    private extractFinishReasonFromChunk;
    private generateOptimizedContinuationPrompt;
    private isVueCode;
    private isHtmlCode;
    private isJavaScriptCode;
    private isArticleContent;
    private generateContinuationSystemRule;
    completionChat(completionChatDto: CompletionChatDto): Promise<any>;
    getModels(getModelsDto: GetModelsDto): Promise<GetModelsResponseDto>;
    private getMessageManager;
    validateRuleType(ruleType: string): boolean;
    getAvailableRuleTypes(): RuleType[];
    getRuleDescription(ruleType: RuleType): string;
    estimateTokens(messages: ChatMessage[]): number;
    runCozeWorkflow(workflowDto: CozeWorkflowRunDto): Promise<CozeWorkflowResponseDto>;
    runCozeWorkflowStream(workflowDto: CozeWorkflowStreamRunDto): Promise<AxiosResponse>;
}
